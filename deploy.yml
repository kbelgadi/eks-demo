 apiVersion: apps/v1
 kind: Deployment
 metadata:
   name: rss-site
 spec:
   replicas: 1
   template:
     metadata:
       labels:
         app: apple-app
     spec:
       containers:
         - name: apple-app
           image: hashicorp/http-echo